# Guidance

**Purpose**: Compose `GuidanceTrack` (section labels + chord lane today; lookahead + note highway later) from `SongUnderstanding` + `InstrumentMIDI`.

**Owner**: Jam Experience attention pool.

**Status**: Skeleton. MVP guidance assembled inline in `tone_forge_api` until this subsystem owns it.
