"""Stems subsystem: produce ``StemSet`` from ``AcquiredAudio``.

Skeleton wrapper. Active separation logic stays in
``tone_forge.stem_separator`` until lifted; this package only owns the
``StemSet`` boundary.
"""

__all__: list[str] = []
