# Stems

**Purpose**: Run separation on `AcquiredAudio` and yield a `StemSet`. Algorithm (Demucs + pan-split) lives in `stem_separator.py` and is frozen.

**Owner**: Platform & Engine attention pool.

**Status**: Skeleton. Wraps existing `stem_separator` until the boundary is fully observed.
