# Lean deps for the RunPod analysis WORKER only — the subset of
# requirements.txt the analysis pipeline actually imports. The worker connects
# OUTBOUND (requests) and never serves HTTP or touches the DB/R2, so the whole
# web/db stack (fastapi, uvicorn, python-multipart, boto3, asyncpg, PyJWT[crypto]
# -> cryptography, httpx, pytest) is dead weight — and it's the compile-heavy,
# slow-to-install part. Dropping it cuts the pod cold-boot pip step notably.
# torch/torchaudio already ship in the base image (skipped as satisfied).
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.26
scipy>=1.11
mutagen>=1.47
requests>=2.31
demucs>=4.0.1
# torch / torchaudio deliberately OMITTED: they ship in the base runpod/pytorch
# image. Listing torch>=2.1 makes pip re-resolve it and try to pull the LATEST
# torch + cuda-toolkit (gigabytes) instead of using the installed build. Deps
# that need torch (demucs, torchcrepe) resolve against the pre-installed one.
# The bootstrap installs torch explicitly only if it's genuinely missing.
torchcrepe>=0.0.23
beat-this>=1.1.0
all-in-one-mps>=1.0.0
pretty_midi>=0.2.10
MIDIUtil>=1.2.1
mido>=1.3
remotezip>=0.12.3
