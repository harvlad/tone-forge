# Frontend (placeholder)

Empty for now. Phase 2 plan:

- Next.js or Vite + React
- Upload widget → POST to `/api/analyze` on the Python backend (FastAPI wrapping `cli.py`)
- Renders a "Chain Card" component: ordered blocks with parameter pills and tweak hints
- Thumbs up / thumbs down on each suggestion → feeds the training data flywheel

Will scaffold once the backend pipeline produces real (non-stub) descriptors.
